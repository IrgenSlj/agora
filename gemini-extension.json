{
  "name": "agora",
  "version": "0.7.1",
  "description": "The trust plane for agentic tooling \u2014 multi-source catalog search, evidence-backed inspection, a policy gate on install, and cross-host stack management, via the agora MCP server (zero-install: npx -y agora-hub mcp).",
  "mcpServers": {
    "agora": {
      "command": "npx",
      "args": ["-y", "agora-hub", "mcp"]
    }
  }
}
