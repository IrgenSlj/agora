{
  "name": "agora",
  "version": "0.8.0",
  "description": "Catch the MCP server that changed after you trusted it \u2014 multi-source catalog search, evidence-backed inspection, a policy gate on install, and cross-host stack management, via the agora MCP server (zero-install: npx -y agora-hub mcp).",
  "mcpServers": {
    "agora": {
      "command": "npx",
      "args": ["-y", "agora-hub", "mcp"]
    }
  }
}
